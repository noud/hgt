<ul class="item-list">
    {% for category in categories %}
        <li>
            <a href="{{ path('category_view', {'id': category.id }) }}" class="listing {% if app.request.attributes.get('id') == category.id %}is-active{% endif %}">
                {{ category.name }}
                <span class="icon"><i class="fa fa-chevron-right"></i></span>
            </a>
        </li>
    {% endfor %}
</ul>



